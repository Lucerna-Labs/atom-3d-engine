# Preserve MMPE's compact, hand-authored style: keep short struct literals, calls, and
# chains on one line up to the line width instead of exploding them across many lines.
max_width = 120
use_small_heuristics = "Max"
