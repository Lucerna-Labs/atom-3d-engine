1. Set up the equation for the given problem.
   - The amount of concentrate is (frac{c}{w} = frac{2}{7})
   - Let's denote the number of parts as (x) where (x = c + w)
   
2. Calculate the value of (x):
   [
   x = left(frac{c}{w}
   right)^{-1}
   ]
3. Solve for the required values using the formulae:
   [
   text{{concentrate}} = left( frac{x cdot 42}{7}, 0.8 times 42
   ]

The solution can be expressed as follows:

- For each part of the mixture, we have (a) parts from the concentrate and (b) (b+1) parts from the water.

Using the provided code to solve this problem step by step:

