# Frontier Crates

This folder is reserved for future high-risk crates.

Do not add these crates to the root workspace automatically. Create them here first when we want to explore architecture without destabilizing the main crate store.

Use the proposed layout in `../CRATE_LAYOUT.md`.
