[package]
name = "mm3e-kit"
version = "0.1.0"
edition = "2021"
license-file = "../LICENSE"
description = "The single primitive kit: all dumb 3-D rendering mechanism, wired from the 8 root atoms."

[dependencies]
